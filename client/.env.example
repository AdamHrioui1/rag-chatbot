VITE_CLERK_PUBLISHABLE_KEY=

# Base URL of the FastAPI RAG service. The browser calls it directly for
# document upload/list/delete (not through the Node server), so it must
# be reachable from the browser, not just from the Node server's host.
REACT_APP_RAG_API_URL=http://localhost:8000
