# Used automatically by `npm run build` (Create React App convention).
# These are just the public addresses of the deployed backend - not
# secrets, safe to commit (anyone using the deployed site would see
# these in their browser's network tab anyway).
REACT_APP_SERVER_API_URL=http://13.51.13.214:5000
REACT_APP_RAG_API_URL=http://13.51.13.214:8000
